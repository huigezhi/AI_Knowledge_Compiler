# 统一行尾：仓库内一律 LF，避免 Windows / macOS / Linux 之间产生整文件 diff。
* text=auto eol=lf

# Windows 批处理必须 CRLF：cmd.exe 解析 LF 换行的 .bat 可能异常（尤其含 label/goto 时）。
*.bat text eol=crlf

# 二进制资源不参与行尾转换与 diff。
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary

# 生成的锁文件不参与 diff 统计。
package-lock.json linguist-generated=true -diff
